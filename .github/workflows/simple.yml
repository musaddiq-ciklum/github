name: Shell Commands

on: [push]

jobs:
  run-shelll-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display a String
        run: echo "Hello World"
      - name: Multilines Script
        run: |
          node -v
          npm -v
